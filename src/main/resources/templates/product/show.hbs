<!DOCTYPE html>
<html lang="en">
<head>
    {{>templates/partials/common}}
    <title>Title</title>
</head>
<body>
    {{>templates/partials/header}}

    <h1>{{product.name}}</h1>
    <p>{{description}}</p>

    <img src="/public/static/img"{{imgid}}>
    <p>{{product.price}}€</p>

    <a href="/seller/{{product.seller.id}}">{{product.seller}}</a>

    <button>Buy</button>

    {{>templates/partials/footer}}
</body>
</html>